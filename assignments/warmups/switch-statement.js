// app.formative.com/join --> LLXPZM
// 1. Rewrite the following `switch` statement using `if` and `else` statements.
// 2. Which do you prefer, `if` or `switch`? Why?

switch (weather) {
  case 'rainy':
    console.log('Remember to bring an umbrella.')
    break
  case 'sunny':
    console.log('Dress lightly.')
  case 'cloudy':
    console.log('Go outside.')
    break
  default:
    console.log('Unknown weather type!')
}
