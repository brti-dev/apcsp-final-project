const joinCode = 'GK8UDQ'

function reverseArray(array) {
  // your code here
  // You must write your algorithm WITHOUT using JavaScript's
  // `reverse` method or CODE.ORG's `append` method.
}

// Tests
console.log(
  'reverseArray([1, 2, 3, 4, 5]) is [5, 4, 3, 2, 1]: ',
  reverseArray([1, 2, 3, 4, 5]) == [5, 4, 3, 2, 1]
)
